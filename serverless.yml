service: spotifysentiment

frameworkVersion: '3.40.0'

useDotenv: true

provider:
  name: aws
  httpApi:
    cors: true
  runtime: python3.11
  region: us-east-1
  memorySize: 512
  timeout: 120
  environment:
    SPOTIFY_CLIENT_ID: ${env:SPOTIFY_CLIENT_ID}
    SPOTIFY_CLIENT_SECRET: ${env:SPOTIFY_CLIENT_SECRET}
    S3_BUCKET_NAME: ${env:S3_BUCKET_NAME}
  iam:
    role:
      statements:
        - Effect: "Allow"
          Action:
            - "comprehend:DetectSentiment"
            - "comprehend:DetectDominantLanguage"
          Resource: "*"
        - Effect: "Allow"
          Action:
            - "s3:PutObject"
            - "s3:PutObjectAcl"
            - "s3:GetObject"
            - "s3:CreateBucket"
            - "s3:HeadBucket"
            - "s3:ListBucket"
          Resource:
            - "arn:aws:s3:::${env:S3_BUCKET_NAME}"
            - "arn:aws:s3:::${env:S3_BUCKET_NAME}/*"

functions:
  api:
    handler: src/lambda_handler.handler
    layers:
      - { Ref: PythonDepsLambdaLayer }
    events:
      - httpApi:
          path: /analyze
          method: post

layers:
  pythonDeps:
    path: layer
    description: "Layer containing lightweight runtime deps for lyrics lookups"
    compatibleRuntimes:
      - python3.11

package:
  patterns:
    - "!node_modules/**"
    - "!.serverless/**"
    - "!.git/**"
    - "!.env"
    - "!.env.example"

